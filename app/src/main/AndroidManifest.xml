<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstip">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstIp">
        <activity android:name=".ui.login"></activity>
        <activity android:name=".ui.CreateAccount" />
        <activity android:name=".ui.TeamActivty" />
        <activity android:name=".ui.last" />
        <activity android:name=".ui.Teams" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>

                <!--  -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>